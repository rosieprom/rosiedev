import Link from 'next/link';
import Navigation from '../components/navigation'

const Header = () => (
  <div>
      <Layout>
          <Navigation />
      </Layout>
  </div>
);

export default Header;